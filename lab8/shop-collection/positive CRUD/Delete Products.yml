info:
  name: Delete Products
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/deleteproduct?id={{created_product_id}}"
  params:
    - name: id
      value: "{{created_product_id}}"
      type: query
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status code is 200", function() {
            expect(res.getStatus(), "Expected status 200 for successful deletion").to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

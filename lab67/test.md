# План unit-тестов для `SubscriptionPlanManager`

| ID    | Метод / функция              | Сценарий                                               | Входные данные / состояние                              | Ожидаемый результат                                          | Что покрывает                                               |
| ----- | ---------------------------- | ------------------------------------------------------ | ------------------------------------------------------- | ------------------------------------------------------------ | ----------------------------------------------------------- |
| TC-01 | `NewSubscriptionPlanManager` | Успешное создание менеджера подписки                   | `plan = free`, `billing != nil`                         | Объект создан, `Plan = free`, `UsedRequests = 0`, ошибки нет | Позитивная ветка конструктора                               |
| TC-02 | `NewSubscriptionPlanManager` | Создание с неизвестным тарифом                         | `plan = premium`, `billing != nil`                      | Возвращается ошибка, объект `nil`                            | Проверка валидации тарифа                                   |
| TC-03 | `NewSubscriptionPlanManager` | Создание без billing-сервиса                           | `plan = free`, `billing = nil`                          | Возвращается ошибка, объект `nil`                            | Проверка обязательной зависимости                           |
| TC-04 | `UpgradePlan`                | Успешное повышение тарифа                              | `free -> pro`, mock billing возвращает `nil`            | `Plan = pro`, billing вызван с `pro`                         | Позитивный upgrade и взаимодействие с mock                  |
| TC-05 | `UpgradePlan`                | Повышение на неизвестный тариф                         | `free -> premium`                                       | Ошибка, `Plan` не изменился, billing не вызван               | Ветка неизвестного тарифа                                   |
| TC-06 | `UpgradePlan`                | Повышение на тот же тариф                              | `pro -> pro`                                            | Ошибка, `Plan` не изменился, billing не вызван               | Проверка правила «новый тариф должен быть выше»             |
| TC-07 | `UpgradePlan`                | Попытка upgrade на более низкий тариф                  | `business -> pro`                                       | Ошибка, `Plan` не изменился, billing не вызван               | Защита от неправильного использования метода                |
| TC-08 | `UpgradePlan`                | Ошибка оплаты при повышении тарифа                     | `free -> pro`, mock billing возвращает ошибку           | Ошибка, `Plan = free`, billing был вызван                    | Бизнес-правило: тариф меняется только после успешной оплаты |
| TC-09 | `DowngradePlan`              | Успешное понижение тарифа                              | `business -> pro`, `UsedRequests = 500`                 | `Plan = pro`, ошибки нет                                     | Позитивный downgrade                                        |
| TC-10 | `DowngradePlan`              | Понижение на неизвестный тариф                         | `business -> student`                                   | Ошибка, `Plan` не изменился                                  | Проверка валидации тарифа                                   |
| TC-11 | `DowngradePlan`              | Понижение на тот же тариф                              | `pro -> pro`                                            | Ошибка, `Plan` не изменился                                  | Проверка правила «новый тариф должен быть ниже»             |
| TC-12 | `DowngradePlan`              | Попытка downgrade на более высокий тариф               | `free -> pro`                                           | Ошибка, `Plan` не изменился                                  | Защита от неправильного использования метода                |
| TC-13 | `DowngradePlan`              | Запрет downgrade из-за превышения лимита нового тарифа | `pro -> free`, `UsedRequests = 500`, лимит `free = 100` | Ошибка, `Plan = pro`                                         | Бизнес-правило ограничения по использованным запросам       |
| TC-14 | `ConsumeRequest`             | Успешное списание запросов                             | `Plan = free`, `ConsumeRequest(10)`                     | `UsedRequests = 10`, ошибки нет                              | Позитивная ветка списания                                   |
| TC-15 | `ConsumeRequest`             | Накопление запросов после нескольких вызовов           | `ConsumeRequest(10)`, затем `ConsumeRequest(15)`        | `UsedRequests = 25`                                          | Мутация состояния несколькими вызовами                      |
| TC-16 | `ConsumeRequest`             | Списание нуля запросов                                 | `ConsumeRequest(0)`                                     | Ошибка, `UsedRequests` не изменился                          | Валидация количества                                        |
| TC-17 | `ConsumeRequest`             | Списание отрицательного количества запросов            | `ConsumeRequest(-5)`                                    | Ошибка, `UsedRequests` не изменился                          | Валидация количества                                        |
| TC-18 | `ConsumeRequest`             | Превышение месячного лимита                            | `Plan = free`, `ConsumeRequest(101)`                    | Ошибка, `UsedRequests = 0`                                   | Проверка верхней границы лимита                             |
| TC-19 | `ConsumeRequest`             | Использование ровно всего лимита                       | `Plan = free`, `ConsumeRequest(100)`                    | Ошибки нет, `UsedRequests = 100`                             | Граничное значение                                          |
| TC-20 | `CanUseFeature`              | Проверка доступности `basic`                           | Все тарифы                                              | Для `free`, `pro`, `business` возвращается `true`            | Общее правило доступности базовой функции                   |
| TC-21 | `CanUseFeature`              | Проверка доступности `analytics`                       | `free`, `pro`, `business`                               | `free = false`, `pro = true`, `business = true`              | Правило функции среднего уровня                             |
| TC-22 | `CanUseFeature`              | Проверка доступности `team_management`                 | `free`, `pro`, `business`                               | Только `business = true`                                     | Business-only функция                                       |
| TC-23 | `CanUseFeature`              | Проверка доступности `priority_support`                | `free`, `pro`, `business`                               | Только `business = true`                                     | Business-only функция                                       |
| TC-24 | `CanUseFeature`              | Неизвестная функция                                    | `feature = unknown`                                     | Возвращается `false`                                         | Ветка `default`                                             |
| TC-25 | `ResetMonthlyUsage`          | Сброс месячного использования                          | `UsedRequests = 80`                                     | `UsedRequests = 0`                                           | Мутация состояния через сброс                               |
